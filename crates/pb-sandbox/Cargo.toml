[package]
name    = "pb-sandbox"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# pb-sandbox owns the OS-level process isolation profile (Module 12).
# Per architecture §4 (v1.5): pb-sandbox is in the "anyone may import" tier
# alongside pb-ipc and pb-config. No pb-* imports; pure type/policy crate.
# Real syscall enforcement (Module 12.1) lives here too once it lands —
# unsafe will be confined to a future `enforce` module per L13.

[dependencies]
serde     = { workspace = true }
toml      = { workspace = true }
thiserror = { workspace = true }
tracing   = { workspace = true }
